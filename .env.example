# HuggingFace Hub (optional - for private datasets or higher rate limits)
HF_TOKEN=your_huggingface_token_here

# LLM API keys (optional - only needed for LLM generation strategy)
OPENAI_API_KEY=your_openai_key_here
ANTHROPIC_API_KEY=your_anthropic_key_here

# Local model path (optional - only needed for local generation strategy)
LOCAL_MODEL_PATH=path/to/your/local/model
